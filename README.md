# eslint-config-jared

## Usage:

#### Add the extension:

```js
// .eslintrc.js
module.exports = {
   extends: "jared"
}

// OR

// .eslintrc
{
   "extends": "jared"
}

```

#### Then install the config:
```
npm i --save-dev eslint-config-jared
```
